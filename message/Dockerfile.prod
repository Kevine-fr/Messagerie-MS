FROM node:22-alpine

WORKDIR /usr/src/app

# Couche dependances (mise en cache tant que package*.json ne change pas).
COPY package*.json ./
RUN npm ci --omit=dev

COPY . .

EXPOSE 3000

CMD ["node", "index.js"]
